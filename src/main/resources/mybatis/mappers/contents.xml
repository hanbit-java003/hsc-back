<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="contents">

	<select id="selectContents" resultType="contentsVO">
		SELECT id, name, main_title, sub_title
		  FROM tbl_contents
		 WHERE id = #{id}
	</select>

	<select id="selectSubmenu" resultType="submenuVO">
		SELECT id, submenu_id, img, title, content, users_img, user_id, together
		  FROM tbl_submenu
		 WHERE id = #{id}
		 ORDER BY submenu_id
	</select>

	<select id="selectSubmenus" resultType="submenuVO">
		SELECT img, title, content, user_no, user_id, users_img, together
		  FROM tbl_submenu
		 ORDER BY user_no
	</select>

</mapper>









